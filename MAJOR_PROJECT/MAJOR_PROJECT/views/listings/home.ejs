    <% layout('../layouts/boilerplate') -%>
    <style>
        * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-color: #f2e6e6;
    color: #333;
    line-height: 1.6;
}

.hero {
    padding: 4rem;
    min-height: 70vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
}

.hero-content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
}

.hero h1 {
    font-size: 4rem;
    line-height: 1.2;
}

.hero span {
    color: #e94444;
}

.hero-image {
    width: 100%;
    height: auto;
    max-height: 600px;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
}

.hero-image:hover {
    transform: translateY(-10px);
}

.volunteer-btn {
    display: inline-block;
    margin-right: 1.2rem;
    padding: 0.5rem 1rem;
    background-color: #ff6b6b;
    color: white;
    text-decoration: none;
    border-radius: 14px;
    font-weight: bold;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    width: fit-content;
}

.volunteer-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(255, 107, 107, 0.3);
}

/* Collaborators section */
.collaborators {
    padding: 4rem;
    text-align: center;
}

.collaborators h2 {
    margin-bottom: 3rem;
    font-size: 2rem;
}

.partners {
    display: flex;
    justify-content: center;
    gap: 4rem;
    align-items: center;
    flex-wrap: wrap;
}

.partner-logo {
    width: 100px;
    height: 100px;
    object-fit: contain;
    transition: transform 0.3s ease;
}

.partner-logo:hover {
    transform: scale(1.1);
}

/* About section */
.about {
    padding: 4rem;
    text-align: center;
}

.about h2 {
    margin-bottom: 2rem;
    font-size: 2rem;
}

.about p {
    max-width: 800px;
    margin: 0 auto;
}

/* Stats section */
.stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    padding: 4rem;
    text-align: center;
}

.stat-card {
    background-color: #e5d6d6;
    padding: 2rem;
    border-radius: 10px;
    transition: transform 0.3s ease;
}

.stat-card:hover {
    transform: translateY(-10px);
}

.stat-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
}

.stat-number {
    font-size: 1.5rem;
    font-weight: bold;
    margin-bottom: 0.5rem;
}

/* Team section */
.team {
    padding: 4rem;
    text-align: center;
}

.team h2 {
    margin-bottom: 3rem;
    font-size: 2rem;
}

.team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 3rem;
    max-width: 1200px;
    margin: 0 auto;
}

.team-member {
    text-align: center;
}

.team-member img {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    margin-bottom: 1rem;
    transition: transform 0.3s ease;
    object-fit: cover;
}

.team-member img:hover {
    transform: scale(1.1);
}

.team-member h3 {
    margin-bottom: 0.5rem;
}

.team-member p {
    color: #666;
}
/* Make sure the body and html elements take up full height */
html, body {
    height: 100%;
    margin: 0;
    padding: 0;
}

footer {
    position: sticky;
    bottom: 0;
    width: 100%;
    background-color: #333;
    color: white;
    text-align: center;
    padding: 10px 0;
    font-size: 16px;
}

marquee {
    color: white;
    font-size: 18px;
    padding: 0 20px;
}

/* Responsive design */
@media (max-width: 1200px) {
    .hero {
        padding: 3rem;
    }

    .hero h1 {
        font-size: 3.5rem;
    }

    .stats {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media (max-width: 992px) {
    .hero {
        grid-template-columns: 1fr;
        text-align: center;
    }

    .hero-content {
        align-items: center;
    }

    .hero h1 {
        font-size: 3rem;
    }

    .hero-image {
        max-height: 400px;
    }

    .partners {
        gap: 2rem;
    }

    .partner-logo {
        width: 80px;
        height: 80px;
    }
}

@media (max-width: 768px) {
    header {
        flex-direction: column;
        padding: 1rem;
    }

    nav {
        margin-top: 1rem;
    }

    .hero {
        padding: 2rem;
    }

    .hero h1 {
        font-size: 2.5rem;
    }

    .collaborators,
    .about,
    .stats,
    .team {
        padding: 2rem;
    }

    .stats {
        grid-template-columns: 1fr;
    }

    .footer-content {
        flex-direction: column;
        gap: 2rem;
        text-align: center;
    }

    .contact-info {
        text-align: center;
    }
}

@media (max-width: 480px) {
    .hero h1 {
        font-size: 2rem;
    }

    .volunteer-btn {
        padding: 0.8rem 1.5rem;
    }

    .team-grid {
        grid-template-columns: 1fr;
    }
}
@keyframes shake {
  0% {
    transform: translateX(0);
  }
  25% {
    transform: translateX(-5px);
  }
  50% {
    transform: translateX(5px);
  }
  75% {
    transform: translateX(-5px);
  }
  100% {
    transform: translateX(0);
  }
}

.shake {
  animation: shake 0.5s ease-in-out; /* Apply the shake animation */
}

    </style>
    <body>
    <section class="hero">
        <div class="hero-content">
            <h1>Uniting <br>everyday<br><span>heroes with</span><br>extraordinary<br>causes</h1>
            <div class="others">

                <a href="/show" class="volunteer-btn">VOLUNTEER</a>
                <a href="/listings/new" class="volunteer-btn">Create
                    Opportunity</a>
            </div>
        </div>
        <div class="hero-image-container">
            <img src="main.png" alt="Volunteers making a difference" class="hero-image">
        </div>
    </section>

    <section class="collaborators">
        <h2>Our Collaborators and Supporter</h2>
        <div class="partners">
            <img src="Unk.png" alt="Scout Logo" class="partner-logo">
            <img src="RedCross.png" alt="Red Cross Logo" class="partner-logo">
            <img src="Uto.png" alt="World Nature Logo" class="partner-logo">
            <img src="Unicef.png" alt="UNICEF Logo" class="partner-logo">
        </div>
    </section>

    <section class="about" id="about">
        <h2>About Us</h2>
        <p>At HeroHub, we connect people with volunteer opportunities that fit their interests and skills. Our goal is
            to make it easy for you to find meaningful ways to give back and make a difference in your community.
            Whether you have a few hours or a lot to give, we're here to help you get started and make a positive
            impact!</p>
    </section>

    <section class="stats">
        <div class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-number">2000+</div>
            <div class="stat-label">volunteers</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">🤝</div>
            <div class="stat-number">100+</div>
            <div class="stat-label">Local Partners</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">📅</div>
            <div class="stat-number">500+</div>
            <div class="stat-label">Events</div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">✉️</div>
            <div class="stat-number">400+</div>
            <div class="stat-label">local invites</div>
        </div>
    </section>

    <section class="team">
        <h2>Our teams</h2>
        <div class="team-grid">
            <div class="team-member">
                <img src="Ellipse 2.png" alt="Team Member 1">
                <h3>Team Member 1</h3>
                <p>CEO</p>
            </div>
            <div class="team-member">
                <img src="Group 23 (1).png" alt="Team Member 2">
                <h3>Team Member 2</h3>
                <p>Designer</p>
            </div>
            <div class="team-member">
                <img src="Group 40.png" alt="Team Member 3">
                <h3>Team Member 3</h3>
                <p>Developer</p>
            </div>
        </div>
    </section>

    <footer >
        <div class="f-info" style="
        display: flex;
        flex-direction: column;
        align-items: center;">
      <footer>
        <marquee behavior="scroll" direction="left">
            <%= news %>
        </marquee>
    </footer>
      </footer>
      <footer style="position: relative; background-color: #aea299;">
            <div class="f-info-socials">
                <a href="https://www.facebook.com/"><i class="fa-brands fa-square-facebook"></i></a>
                <a href="https://www.instagram.com/"> <i class="fa-brands fa-square-instagram"></i></a>
                <a href="https://www.linkedin.com/"> <i class="fa-brands fa-linkedin"></i></a>
            </div>
            <div>&copy;HeroHub</div>
            <div class="f-info-links">
                <a href="/privacy">Privacy</a>
                <a href="/terms">Terms</a>
            </div>
        </div>
    </footer>
    
    
</body>
<script>
 document.addEventListener("DOMContentLoaded", () => {
  let statsAnimated = false; // Flag to ensure the animation runs only once

  const updateCount = (stat) => {
    const target = +stat.textContent.replace("+", ""); // Get target value
    stat.textContent = "0"; // Initialize the number at 0

    const animate = () => {
      const current = +stat.textContent.replace("+", ""); // Current value
      const increment = Math.ceil(target / 50); // Increment value for animation

      if (current < target) {
        stat.textContent = `${Math.min(current + increment, target)}+`; // Update value
        stat.classList.add("shake"); // Add the shake effect
        setTimeout(() => {
          stat.classList.remove("shake"); // Remove shake effect after animation
        }, 500); // Duration of the shake (matches the CSS animation duration)
        setTimeout(animate, 10); // Animation speed
      } else {
        stat.textContent = `${target}+`; // Final value
      }
    };

    animate();
  };

  // Create an Intersection Observer to trigger the stats animation when the section is visible
  const observer = new IntersectionObserver(
    (entries) => {
      const statsSection = entries[0];
      if (statsSection.isIntersecting && !statsAnimated) {
        statsAnimated = true; // Prevent re-triggering the animation
        const statNumbers = document.querySelectorAll(".stat-number");
        statNumbers.forEach(updateCount); // Start the counters
      }
    },
    { threshold: 0.5 } // Trigger when 50% of the section is visible
  );

  const statsSection = document.querySelector(".stats");
  if (statsSection) {
    observer.observe(statsSection); // Observe the stats section
  }
});

</script>

